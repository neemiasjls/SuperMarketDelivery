# Variáveis lidas pelo docker-compose.yml para criar o container do PostgreSQL.
# Copie este arquivo para ".env" (na raiz) e defina uma senha.
# NUNCA commite o .env com valores reais — ele já está no .gitignore.

POSTGRES_DB=braga_db
POSTGRES_USER=braga_user
POSTGRES_PASSWORD=defina_uma_senha
